# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
db.sqlite3
db.sqlite3-journal
*.sqlite3
dev.sqlite3

# Node / frontend
node_modules/
dist/
*.tsbuildinfo
frontend/vite.config.d.ts
frontend/vite.config.js

# Env / secrets
# `deploy/.env` 只匹配那一个确切路径 —— deploy/.env.prod 是漏网的，
# 而部署文档正让人建这个文件。用通配符兜住所有 .env*，
# 再把 .example 模板显式放回来（它本来就该进版本库）。
.env
.env.*
deploy/.env
deploy/.env.*
!.env.example
!deploy/.env.prod.example
*.local.env

# Docker
.docker/

# Logs / runtime
*.log
output/*.log
.playwright-cli/
staticfiles/
backend/staticfiles/
media/
# （backend/ 已随 Django 退役删除；媒体目录现为顶层 media/）
celerybeat-schedule*

# Scratch / research artifacts (not part of the project)
output/
outputs/
tmp/
tools/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
dump.rdb
backend/dump.rdb
# Go 构建产物
backend-go/tms-gateway

# TLS 私钥绝不进版本库（deploy/gen-dev-cert.sh 生成的自签证书也一样）
deploy/certs/
backend-go/internal/config/releasecheck_tmp/
