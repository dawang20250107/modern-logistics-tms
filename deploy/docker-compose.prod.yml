# 生产编排（本地预演 / 腾讯云 CVM 单机起步）。
#   docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up -d --build
# 生产务必设置强随机 DJANGO_SECRET_KEY（签发 JWT 与司机端令牌都用它）与真实
# CORS 白名单，并在前置网关启用 TLS。托管 PG/COS 时改用对应 URL 即可。
#
# Django 已退役：网关是唯一的应用进程；schema 由它内嵌的迁移器建立，
# 上报削峰与掉线扫描都在进程内，不再需要 Redis 与 Celery。

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-tms}
      POSTGRES_USER: ${POSTGRES_USER:-tms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tms_prod_pwd}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tms} -d ${POSTGRES_DB:-tms}"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: always

  gateway:
    build:
      context: ../backend-go
      dockerfile: Dockerfile
    image: tms-gateway:prod
    environment:
      GO_LISTEN_ADDR: ":8000"
      DATABASE_URL: ${DATABASE_URL:-postgres://tms:tms_prod_pwd@db:5432/tms}
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-CHANGE-ME-IN-PRODUCTION-USE-32BYTES+}
      DJANGO_DEBUG: "false"
      DJANGO_CORS_ORIGINS: ${DJANGO_CORS_ORIGINS:-}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-}
      MEDIA_ROOT: /app/media
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_BASE_URL: ${DEEPSEEK_BASE_URL:-https://api.deepseek.com}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL:-deepseek-v4-pro}
      ALERT_SPEED_LIMIT_KMH: ${ALERT_SPEED_LIMIT_KMH:-90}
      DEVICE_OFFLINE_MINUTES: ${DEVICE_OFFLINE_MINUTES:-10}
      AMAP_KEY: ${AMAP_KEY:-}
      AGENT_MCP_SERVERS: ${AGENT_MCP_SERVERS:-}
      MQTT_HOST: ${MQTT_HOST:-}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_TOPIC: ${MQTT_TOPIC:-tms/telemetry/#}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
    volumes:
      - media:/app/media
    depends_on:
      db: { condition: service_healthy }
    restart: always

  web:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.frontend
    image: tms-frontend:prod
    ports:
      - "80:80"
    depends_on:
      - gateway
    restart: always

volumes:
  pgdata:
  media:
